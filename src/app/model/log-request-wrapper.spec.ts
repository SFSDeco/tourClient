import { LogRequestWrapper } from './log-request-wrapper';

describe('LogRequestWrapper', () => {
  it('should create an instance', () => {
    expect(new LogRequestWrapper()).toBeTruthy();
  });
});
